export class Presentation {
    name: string
    presentation: string[]

    constructor(
        name: string,
        presentation: string[],
    ) {
        this.name = name;
        this.presentation = presentation
    }
}